Ты — senior frontend-разработчик. У меня есть SPA-проект «Матеон» на чистом JS (один index.html, один __check.js). В массиве THEORY_LIBRARY уже описаны фигуры: sphere, ball, cube, pyramid, cylinder, triangle, rectangle, quadrilateral. Приложение использует state-объект, рендеринг через шаблонные строки и делегирование событий.

🎯 ЗАДАЧА:
Добавь интерактивный 3D-просмотрщик для геометрических фигур. При открытии статьи в разделе «Теория» рядом с заголовком должна появиться кнопка «🔍 3D». По клику открывается модальное окно с:
• Вращающейся 3D-моделью фигуры (OrbitControls: вращение, зум, панорамирование)
• Ползунками для изменения параметров (радиус, высота, сторона и т.д.)
• Живым расчётом и отображением площади поверхности (S) и объёма (V)
• Корректным соответствием типу фигуры из THEORY_LIBRARY

🛠 ТЕХНИЧЕСКИЕ ТРЕБОВАНИЯ:
1. Подключи Three.js через CDN в index.html используя ES-модули и importmap:
   <script type="importmap">
     { "imports": { "three": "https://unpkg.com/three@0.160.0/build/three.module.js", "three/addons/": "https://unpkg.com/three@0.160.0/examples/jsm/" } }
   </script>
   Импортируй OrbitControls как `import { OrbitControls } from 'three/addons/controls/OrbitControls.js';` внутри <script type="module">.
2. Интегрируй в существующую архитектуру:
   • Добавь в state.modal.type поддержку 'figure_3d'
   • Расширь renderModal() для рендеринга canvas-контейнера, ползунков и блока с формулами
   • Создай функцию init3DViewer(figureId, container, onUpdateUI), которая:
     - Инициализирует сцену, камеру, рендерер, свет
     - Сопоставляет figureId с Three.js геометриями
     - Добавляет OrbitControls и анимационный цикл
     - Возвращает объект с методами updateParams(params) и dispose()
3. Маппинг фигур (примерный):
   • figure_sphere / figure_ball → THREE.SphereGeometry
   • figure_cube → THREE.BoxGeometry
   • figure_cylinder → THREE.CylinderGeometry
   • figure_pyramid → THREE.ConeGeometry (4 стороны) или кастомная BufferGeometry
   • 2D-фигуры → THREE.ExtrudeGeometry с глубиной 0.1 или PlaneGeometry с сеткой
4. UI ползунков: генерируй динамически в зависимости от типа фигуры (radius, height, width, length и т.д.). При изменении обновляй геометрию через renderer.dispose() + новую геометрию или через scale/morph, пересчитывай S и V по формулам и обновляй DOM.
5. Очистка памяти: при закрытии модалки вызывай dispose() для geometry, material, renderer и удаляй canvas из DOM. Останавливай requestAnimationFrame.
6. Адаптивность: canvas должен подстраиваться под контейнер, обрабатывай window.resize.
7. Стиль кода: vanilla JS, шаблонные строки, делегирование событий. Не меняй логику существующих функций, только добавляй новые и расширяй renderModal/renderTheoryView.
8. Формулы расчёта должны строго соответствовать данным из THEORY_LIBRARY.

📤 ФОРМАТ ОТВЕТА:
1. Обновлённый <head> index.html с подключением Three.js
2. Новые/изменённые функции из __check.js:
   • renderTheoryView() (добавлена кнопка 3D)
   • renderModal() (кейс для 'figure_3d')
   • init3DViewer() и логика параметров
   • Функции расчёта S/V для каждой фигуры
   • Обработчики событий и очистка
3. Краткая инструкция, куда вставлять код в текущем файле.
4. Убедись, что код самодостаточен, не использует фреймворки и не ломает текущий SPA-роутинг.

Начинай генерацию. Пиши код с комментариями на русском.









Ты — senior frontend-разработчик. У меня SPA «Матеон» на чистом JS (index.html + __check.js). Архитектура: state-объект, рендеринг через шаблонные строки и app.innerHTML, делегирование событий в document.addEventListener. Проект полностью рабочий, но требует доработки UX, доступности (a11y) и стабильности рендеринга.

🎯 ЗАДАЧА:
Внедри системные улучшения, не ломая текущую логику, роутинг и localStorage. Исправь: потерю фокуса при перерендере, отсутствие семантики для скринридеров, пустые экраны без подсказок и длинные смешанные шаблоны.

🛠 ТЕХНИЧЕСКИЕ ТРЕБОВАНИЯ:
1. Управление фокусом и скроллом при рендеринге:
   • Перед app.innerHTML = ... сохраняй document.activeElement (или его data-атрибут/id) и window.scrollY.
   • После рендера восстанавливай скролл и фокус на тот же интерактивный элемент. Если его нет в DOM — переводи фокус на [data-nav="home"] или первый <h2>.
   • Оборачивай восстановление в requestAnimationFrame для плавности.
2. Доступность (a11y):
   • Динамически создай или найди в DOM регион: <div id="a11y-live" aria-live="polite" aria-atomic="true" style="position:absolute;left:-9999px;width:1px;height:1px;overflow:hidden;"></div>
   • Модифицируй showToast() и вывод lessonFeedback: записывай текст в этот регион (только для скринридеров), а не только в визуальный блок.
   • В openModal()/closeModal() реализуй focus trap: Tab/Shift+Tab не должны выходить за пределы .modal-layer. При открытии — focus на первый <input> или <button>. При закрытии — возврат фокуса на элемент, который вызвал модалку (сохраняй его в state.modal.triggerElement).
3. Пустые состояния (Empty States):
   • В renderHome(), renderCourseView() добавь проверку: если appData.courses.length === 0 или course.modules.length === 0, рендерь <div class="empty-state"> с иконкой, текстом «Пока нет данных» и кнопкой действия (для teacher: «Создать первый курс/модуль», для student: «Вернуться на главную»).
   • В renderTheoryView() обработай случай, когда THEORY_LIBRARY пуст (маловероятно, но защити от краша).
4. Рефакторинг шаблонов:
   • Вынеси длинные шаблонные строки в отдельные чистые функции: tpl.courseCard(), tpl.moduleRow(), tpl.lessonRow(), tpl.achievementCard(), tpl.emptyState().
   • Размести их в __check.js в блоке // === UI TEMPLATES === в начале или конце файла.
   • Не меняй бизнес-логику, только структурируй рендеринг. Используй существующие CSS-классы проекта.
5. Сохранение архитектуры:
   • ❌ Не добавляй фреймворки, сборщики, virtual DOM или внешние зависимости.
   • ✅ Сохрани делегирование событий, state, appData, localStorage и все текущие функции.
   • ✅ Код должен работать сразу после вставки без дополнительной настройки.

📤 ФОРМАТ ОТВЕТА:
1. Готовый код с маркерами // INSERT HERE и // REPLACE FROM...TO... для точной вставки.
2. Чёткая пошаговая инструкция: «Замени функцию X на новую», «Добавь этот блок в начало __check.js», «Вставь этот div в index.html перед </body>».
3. Краткое пояснение (3–4 пункта), как работает сохранение фокуса и focus-trap в vanilla JS без библиотек.
4. Убедись, что после применения: прогресс не теряется, модалки не ломают навигацию, пустые массивы отображаются корректно, а скринридеры получают фидбек.

Начинай генерацию. Пиши код на русском, с комментариями для быстрой интеграции.